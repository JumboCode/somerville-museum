import { useState } from "react";

const DeleteItemButton = () => {

    return(
        <button
            // className="text-center border p-2 rounded bg-gray-100 
            // hover:bg-gray-200"
            // onClick={() => alert("BUG: Replace this with someth)ing else...")}
        >
            asdufjajsdkfajksd
        </button>    
    );
}

export default DeleteItemButton;